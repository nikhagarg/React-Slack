import React from 'react'
import './Thread.css';

function Thread() {
    return (
        <div className="thread">
            <div class="thread_container">
                <h5>Add messages and files to come back to later.</h5>
                <p>Mark your to-dos or save something for another time. Only you can see your saved items, so use them however you’d like.</p>
            </div>
        </div>
    )
}

export default Thread;
